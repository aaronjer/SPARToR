#Required packages:
#  libsdl1.2-dev
#  libsdl-net1.2-dev
#  libsdl-image1.2-dev
EXE_NAME = spartor_win32.exe
OBJS = main.o font.o console.o command.o net.o host.o client.o input.o video.o mod.o sjdl.o
OBJSRES = icon.o
CC = gcc
WINDRES = windres
FLAGS = -g -Wall -mwindows
LIBS = -L /local/lib -lmingw32 -lSDLmain -lSDL -lSDL_net -lSDL_image -lopengl32 -lglu32 -lm
INC = -I /usr/local/include/SDL



$(EXE_NAME): $(OBJS) $(OBJSRES)
	$(CC) -o $@ $(OBJS) $(OBJSRES) $(FLAGS) $(INC) $(LIBS) $(XLIBS)
	-rm $(OBJS) $(OBJSRES)

$(OBJS):
	$(CC) -o $@ -c $(FLAGS) $(INC) $*.c

.rc.o:
	$(WINDRES) $^ -o $@
%.o : %.rc
	$(WINDRES) $^ -o $@

clean:
	-rm $(OBJS) $(OBJSRES)
